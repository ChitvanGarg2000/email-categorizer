[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ChitvanGarg2000", email = "chitvangarg0305@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.22.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
